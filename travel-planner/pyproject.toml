[project]
name = "travel-planner-skill"
version = "1.0.0"
description = "Create and update travel itineraries in Notion with bilingual support"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
python-preference = "managed"
